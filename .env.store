# Per-stack config for the store instance. See .env.warehouse for the
# same fields with the warehouse values.

INSTANCE_NAME=store
FRONTEND_TITLE=Duck Store

BACKEND_HOST_PORT=4002
FRONTEND_HOST_PORT=5174

MONGO_DB_NAME=store
